cmake_minimum_required(VERSION 3.2)

project(sibira)

# add_subdirectory(assembler)
add_subdirectory(bundleParser)
# add_subdirectory(disassembler)
add_subdirectory(instrumentation)
add_subdirectory(lib)